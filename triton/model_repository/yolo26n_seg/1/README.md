Place the TensorRT engine file for YOLO26n-seg in this folder.

Expected file name example:

- `model.plan`

The export/build flow is intentionally separate so you can choose Jetson-side or EC2-side engine build later.
